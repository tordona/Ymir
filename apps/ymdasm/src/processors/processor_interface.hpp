#pragma once

class ICommandProcessor {
public:
    // TODO: define public interface for command processors
    // - receive named options (short and long) + argument(s)
    // - set or clear named flag (short and long)
    // - receive positional argument
};
